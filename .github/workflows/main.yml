on: [pull_request]

jobs:
  pull-request-auto-assign:
    runs-on: ubuntu-latest
    name: Pull Request auto-assignment
    steps:
      - name: Pull Request auto-assignment
        id: perform-assignment
        uses: marcel-ribeiro/pull-request-auto-assign-action@1.0.2-SNAPSHOT
        with:
          assignee: 'marcel-ribeiro'
